/*
  # Create Test Accounts (SANITIZED VERSION)

  Creates test accounts for demonstration.
  
  NOTE: Passwords should be set manually in production.
  This file has been sanitized for public repositories.
*/

-- Test accounts should be created manually with secure passwords
-- DO NOT commit actual passwords to version control

-- Example structure (passwords removed):
-- INSERT INTO auth.users (email, encrypted_password, ...)
-- VALUES ('user@example.com', crypt('SECURE_PASSWORD_HERE', gen_salt('bf')), ...);